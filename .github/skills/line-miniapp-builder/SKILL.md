---
name: line-miniapp-builder
description: "Use when: building a local-first LINE mini app style web app."
argument-hint: "承認済みの計画"
---

# LINEミニアプリ実装スキル

承認済み計画に沿って `app/` に実装します。基本はVite + React + TypeScript、LIFFモック、モバイルファースト。`npm run build` と可能な範囲のブラウザ確認まで行います。
